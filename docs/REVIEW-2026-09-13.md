# GitHub Pages 점검 및 수정 기록 — 2026-09-13

대상: `Lorkhan-0307/Lorkhan-0307.github.io`, `main`, 시작 커밋 `dec863f`.
작업 시작 시 원격을 fetch하여 로컬과 origin/main이 일치하고 기존 변경사항이 없음을 확인했다.

## 점검 범위

기존 추적 파일 348개의 목록을 확인하고, 사이트 설정·배포 흐름·템플릿·JavaScript·SCSS·게시글 메타데이터·리소스 참조를 검사했다. 게시글 143개를 포함한 전체 사이트를 생성하고 최종 HTML 330개의 내부 링크와 앵커를 검사했다. 브라우저에서는 한국어 검색, 모바일 검색 취소, 수식, 다이어그램, 댓글 스크립트와 홈 화면을 확인했다.

게시글의 학습 내용 전체에 대한 사실 확인이나 모든 예제 코드의 정답성 검증은 이번 사이트 동작 점검에 포함하지 않았다.

## 수정 내용

| 문제 | 수정 |
| --- | --- |
| 한국어 설정 `ko`가 테마 언어 파일과 불일치하여 UI가 영어로 표시됨 | `ko-KR`로 수정 |
| C, C++, c++, c# 태그가 `/tags/c/`를 덮어씀 | C는 유지하고 C++는 `cpp`, C#은 기존 표기 `c-sharp`로 통일 |
| 대소문자·공백 차이로 태그/카테고리 아카이브 충돌 | algorithm, oop, data-structure, design-principles 표기 통일 |
| MathJax의 오래된 CDN 및 중복 로딩 | 테마의 MathJax 3 로더로 통합하고 기존 `use_math` 지원 유지 |
| Mermaid가 HTML 요소 생성 전에 실행되고 두 번 로드됨 | 본문 이후의 테마 로더만 사용 |
| 다이어그램이 있는 글 2개에 활성화 설정 누락 | C# 메모리 관리, OOP 글에 `mermaid: true` 추가 |
| DI, GC, IDisposable, OOP 다이어그램의 구문 오류 | 괄호가 있는 레이블을 인용하고, 줄바꿈 및 잘못 섞인 Markdown 링크 수정 |
| 고정 다크 테마에서 존재하지 않는 ModeToggle 참조 가능 | Mermaid/Giscus 메시지 처리에 존재 여부 검사 추가 |
| 댓글 스크립트가 글 레이아웃에 하드코딩되어 댓글 설정을 무시 | 기존 저장소·카테고리·색상 설정을 보존하여 Giscus 설정과 테마 댓글 경로로 통합 |
| 모바일 검색 취소 후 검색어가 남음 | input.textContent 대신 input.value 초기화 |
| 목차가 꺼진 글에서도 tocbot을 실행하여 오류 발생 | 목차 DOM이 있는 경우에만 초기화 |
| 실제 생성 주소와 다른 게시글 링크 2개 | Jekyll post_url로 연결 |
| RSS의 없는 로고 참조 및 HTML 엔티티 중복 처리 | 실제 아이콘 파일 사용, 필드별 XML escaping 적용 |
| 글 미리보기의 `&amp;` 중복 표시 | escape_once 적용 |
| 예제 Twitter 계정 링크가 실제 연락처로 노출됨 | 예제 계정과 해당 연락처 제거 |
| Windows 시간대 라이브러리 누락으로 빌드 실패 | Windows용 tzinfo/tzinfo-data 의존성 추가 |
| CSS 사용자 정의 속성에 Sass 변수가 문자 그대로 출력됨 | 보간 문법 적용 |
| Sass 선언 순서 경고 | 기존 출력 의미를 유지하도록 선언 순서 정리 |
| 출력 폴더 비동기 삭제와 빌드가 경합할 수 있음 | Rollup/PurgeCSS의 삭제·생성 순서 보장 |
| viewport 문법 오류 및 확대 제한 | 유효한 viewport 설정으로 정리 |
| 개발 의존성 보안 경고 21건 | Terser 플러그인·semantic-release 및 하위 의존성 갱신, npm audit 0건 확인 |
| JavaScript 소스 변경을 배포 빌드에서 검사하지 않음 | Node 24, npm ci, 프런트엔드 빌드·테스트를 배포 workflow에 추가 |

`package-lock.json`을 추적하여 검증한 의존성 버전을 고정했다. JS/CSS 배포 산출물도 재생성했다. Node 요구사항은 갱신된 도구에 맞춰 `^22.14.0 || >=24.10.0`으로 명시했다.

## 검증 결과

| 검사 | 결과 |
| --- | --- |
| npm ci --ignore-scripts | 통과 |
| npm run build | JS 8개 번들 및 CSS 빌드 통과 |
| npm test | SCSS 검사 및 목차 회귀 테스트 2개 통과 |
| npm audit | 취약점 0건 |
| JEKYLL_ENV=production bundle exec jekyll build | 통과, 아카이브 충돌 및 Sass 경고 없음 |
| bundle exec ruby tools/check-site.rb | HTML 330개, 내부 링크·앵커, JSON, XML, 아카이브 이름 검사 통과 |
| 브라우저 수식 검사 | 페이징 글 수식 4개 렌더링, MathJax 엔진 중복 없음 |
| 브라우저 다이어그램 검사 | 해당 게시글 9개의 다이어그램 11개 모두 SVG 렌더링, 구문 오류 없음 |
| 브라우저 검색 검사 | 한국어 검색 결과 확인, 모바일 취소 후 검색어가 비워지고 결과 영역 숨김 확인 |
| 브라우저 댓글 검사 | Giscus 클라이언트 스크립트 1개 로드 확인; 실제 댓글 작성은 수행하지 않음 |
| git diff --check | 통과 |

새 `tools/check-site.rb` 검사는 배포 workflow와 `tools/test`에도 연결했다. 특히 아카이브 충돌은 Jekyll이 경고만 출력하고 성공 종료하는 문제이므로 별도로 실패 처리한다.

## 검증 한계와 현재 상태

- 이 PC에서는 HTMLProofer의 libcurl DLL 로딩 문제로 기존 HTMLProofer 명령을 완료하지 못했다. 별도 내부 링크/앵커 검사는 통과했으며, Linux 배포 workflow의 기존 HTMLProofer 단계는 유지했다.
- 외부 링크 전체의 응답 상태와 실제 GitHub Actions 실행 결과는 검증하지 않았다.
- 정보 탭에는 테마의 초기 안내 문구가 남아 있다. 개인 소개 내용은 임의로 작성하지 않았다.
- 수정은 로컬 작업 폴더에 있으며 커밋·푸시·배포는 수행하지 않았다.
